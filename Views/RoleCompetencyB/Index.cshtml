@model IEnumerable<Tikti.Models.RoleCompetencyB>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Organization_view.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ComptencyBNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RoleOpportunityNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ComptencyBNavigation.CompetencyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoleOpportunityNavigation.City)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoleCompetencyBid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoleCompetencyBid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoleCompetencyBid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
